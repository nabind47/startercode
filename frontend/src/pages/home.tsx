import CustomFlow from "@/components/global/custom-flow";
import CustomHeader from "@/components/global/custom-header";

const HomePage = () => {
  return (
    <div className="p-2">
      <CustomHeader />

      <CustomFlow />
    </div>
  );
};

export default HomePage;
